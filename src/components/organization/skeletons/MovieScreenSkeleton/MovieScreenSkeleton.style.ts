import { StyleSheet } from 'react-native';

export const movieScreenSkeletonStyles = StyleSheet.create({
  container: {
    height: 500,
    flexWrap: 'wrap',
    flex: 1,
    flexDirection:'row',
  },
});