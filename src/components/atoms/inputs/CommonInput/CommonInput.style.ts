import { StyleSheet } from 'react-native';

export const commonInputStyles = StyleSheet.create({
  input: {
    marginTop: 20,
    marginBottom: 10,
    padding: 15,
    height: 10,
    borderRadius: 10,
    borderWidth: 2
  }
});